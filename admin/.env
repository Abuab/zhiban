# 接口前缀（与后端全局前缀 /api 对齐；生产由宿主 Nginx 同域反代，开发由 Vite 代理转发）
VITE_API_BASE_URL=/api
# 本地开发时 Vite 代理的后端地址（仅 dev 使用，生产不走代理）
VITE_DEV_PROXY_TARGET=https://m.arvine.cn
# 品牌名兜底值（仅用于接口下发前/下发失败时的降级展示；运行时真源是服务端 sys_config.brand.name）
VITE_BRAND_NAME=知伴
# 后台自身称谓（与品牌名组合为浏览器标题，如「知伴 管理后台」）
VITE_ADMIN_NAME=管理后台
