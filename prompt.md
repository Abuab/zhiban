# DeepSeek/Trae 开发驱动提示词包 v1.0

## 使用方式（重要）
1. 将《知伴-全套交付文档-v1.0.md》放到项目仓库的 `/docs/constitution.md`（保持单文件，DeepSeek 上下文够用）
2. 严格按下面的阶段顺序投喂，每阶段结束有【人审门禁】，你确认后才发下一阶段提示词
3. 所有提示词原样复制，【】内按实际情况替换

---

## 阶段 0｜总控提示词（项目开始前粘贴一次，作为长期规则）

```
你是一名资深全栈工程师，负责开发「知伴」微信小程序（uni-app + Vue3 + NestJS + MySQL + Redis）。
项目根目录 /docs/constitution.md 是唯一产品规格来源（下称"规格"），其内部按优先级排列：
宪法 > 规范增补 > PRD > 边界总表。当文件间冲突时按此顺序裁决，并在输出中说明你的裁决依据。

铁律（任何时候不得违反）：
1. 不得擅自修改规格中的任何规则（定价、文案红线、计分逻辑、可见性规则）。
   若你认为规格有误或不可实现，停下来向我提问，禁止自行其是。
2. 规格没写清的地方：列出你的假设和选项让我选，禁止猜测后直接开发。
3. 每完成一个开发单元，输出：变更文件清单 + 实现对应规格哪一条（引用章节号）+
   边界总表落实情况（编号）。
4. 需要偏离规格做技术决策时，先写一份 ADR（背景/选项/决定/后果）让我确认。
5. 代码注释、提交信息使用中文。接口、数据库字段命名用英文。
6. 任何涉及钱、权益、隐私的代码，写完后自查一遍"恶意用户会怎么攻击这里"。

先不要写代码。第一步：通读 /docs/constitution.md，输出：
① 你对产品的三句话理解；② 你识别出的 5 个最大技术风险；
③ 你发现的规格内部矛盾或缺失（如有）；④ 建议的开发阶段拆分。
等我确认后再进入下一步。
```

【人审门禁】：检查它的理解是否跑偏（重点看：三层可见性、劝和基调、快照机制、支付幂等）

---

## 阶段 1｜架构设计（不写业务代码）

```
基于已确认的规格，产出架构设计，禁止写业务代码：
1. 系统架构图（mermaid）：与规格《基础设施与部署方案》一致（Nginx/NestJS×2/MySQL/Redis/COS）
2. 数据库表设计（建表 SQL）：覆盖规格中所有数据模型（用户、量表配置、答案快照、邀请、
   报告、权益、订单、锦囊CMS、运营位、审计日志）
3. 模块划分与依赖关系图（mermaid）
4. 关键时序图（mermaid）至少三张，必须与 PRD-002 状态机逐节点对齐：
   a. 支付到账与权益发放（含回调幂等、查单补单）
   b. 双人邀请全流程（invite_created→opened→consent→answering→completed→report_unlocked，
      含 expired/declined/cancelled 分支）
   c. 报告生成与三层可见性渲染（完整版/基础版/分享版的数据过滤）
5. 配置化落地清单：规格《配置项注册表》7 个配置域各自落在哪些表/哪些管理后台页面
6. ADR-001：技术选型决策记录（含你最终选择的规则引擎/模板引擎方案及理由）

输出后停下，等我确认。
```

【人审门禁】：时序图对照 PRD-002 状态机逐节点核对；表设计对照边界总表 C/E/F 域

---

## 阶段 2｜分模块开发（每模块一条提示词，完成一个再发下一个）

推荐顺序（依赖关系已排好）：

```
模块 1：项目脚手架
按架构设计初始化：uni-app 小程序端 + NestJS（模块结构、鉴权中间件、统一响应格式、
环境配置管理、数据库连接、Redis、日志、全局异常处理、rate limit 中间件）。
完成标准：健康检查接口可访问，docker-compose 可一键起 MySQL/Redis。
```

```
模块 2：微信登录与账号体系
按规格 A 域边界表：wx.login 换 openid、用户建档、登录态管理。
完成标准：小程序端可登录，未登录访问受保护接口返回 401。
```

```
模块 3：量表引擎（配置化的核心）
实现规格"题库 v1.0"全部数据模型与计分规则（第三部分 8 条规则），
含：量表配置表导入 /docs/constitution.md 中的 79+24 题、计分服务（反向题/维度均分/
分歧判定/差值分级/底线触发/低质量标记）、报告模板引擎（占位符渲染）。
完成标准：单元测试覆盖 8 条计分规则全部场景；导入的题库与规格文件逐题一致（出题数核对）。
```

```
模块 4：单人测评流程
答题流程（断点续答 B1、进度、敏感维度前置同意 B7）、简版报告生成、付费墙锁定占位。
完成标准：一个人可以从头到尾测完并看到简版报告。
```

```
模块 5：双人邀请与对比报告
严格按 PRD-002：状态机、邀请码绑定首个授权人 C1、答案复用 C3、30 天过期 C4、
换人 C7、三层可见性 R3、底线题双向提示 R5、复用历史快照 B8。
完成标准：两个测试账号完整走通流程；越权测试（A 访问 B 的报告）返回 403。
```

```
模块 6：支付与权益
严格按 PRD-005：预下单、回调验签+幂等、每日对账任务、恢复购买按钮 E1、
退款、兑换码（iOS 过渡）。金额以后端为准 E4。
完成标准：沙箱环境走通支付-回调-权益-退款全链路；伪造回调测试被拒。
```

```
模块 7：锦囊卡片流与专属卡
按《锦囊卡片流 v1.0》：CMS 数据格式入库、卡片渲染（坑卡/话术卡/演练卡/专属卡）、
长按复制、AI 专属卡生成（调用大模型 API，prompt 用规格 §9.2 模板，
生成一次缓存、禁词校验、失败降级）。
完成标准：8 个议题可浏览；专属卡仅付费后生成且二次进入读缓存。
```

```
模块 8：管理后台（Vue3）
按《配置项注册表》：量表/计分/报告模板/商品/内容/运营位/开关 7 个配置域的
增删改查 + 数据看板 + 对账补单工具。
完成标准：运营人员改一道题、改价格、下架一篇锦囊均无需发版（G1 验收）。
```

【人审门禁·每模块后】：让它自己跑一遍该模块涉及的边界总表条目并演示

---

## 阶段 3｜全链路联调与验收

```
联调 checklist（逐项演示给我）：
1. 完整用户旅程：新用户登录→单人测评→邀请伴侣→对方答题→¥8 支付→
   三层报告各自正确→¥3 解锁锦囊→专属卡生成
2. 边界总表 v0.1 全部 45+ 条：逐条说明处理位置，现场演示 P0 条目
3. 合规自检：全文搜索代码与配置中的 P7 禁词；模拟微信审核视角自查
4. 性能：报告生成 <10 秒；支付回调处理 <2 秒
5. 输出部署文档：按《基础设施与部署方案》在 4C4G 单机的完整部署步骤
```

---

## 给 DeepSeek 的通用约束（出问题就追加）

- "重读规格第 X 章再回答，你的实现与之矛盾"（它开始自作主张时）
- "先列出你计划修改的所有文件，我确认后你才动手"（它要大面积改动时）
- "输出你对规格这条规则的理解，再动手"（涉及钱/可见性/计分时）
