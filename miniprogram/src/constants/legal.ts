/**
 * 法务类端上常量：《双人数据处理说明》（配对级同意）
 *
 * 规格依据：ADR-012 决策 2 / 决策 3（配对级同意留证、发起方创建前同意）、
 *          ADR-011 决策 3 / 决策 5（删除范围与保留的三类留痕）、
 *          constitution.md 第 225 / 615 行（R8 知情同意：双方答题前均须勾选）。
 *
 * 与 `constants/privacy.ts` **并存不合并**：隐私政策是账号级链路，本文件只服务配对级同意，
 *   合并会让两条互不相关的合规链路绑在一起（ADR-012 变更清单的原话）。
 *
 * ⚠️ 版本号与说明正文在**三处同源**，任何改动必须三处同步：
 *   ① 本文件
 *   ② 服务端 `server/src/modules/invite/invite.constants.ts`（`INVITE_DATA_CONSENT_VERSION` / `INVITE_CONSENT_TEXT`）
 *   ③ 说明正文文档 `docs/invite-data-notice.md`
 *
 * 遗留项：正文文档尚未定稿，缺运营主体全称 / 生效日期 / 客服联系方式，故本页暂不含运营主体署名。
 */

/**
 * 说明文案版本号（与服务端 `INVITE_DATA_CONSENT_VERSION`、`docs/invite-data-notice.md` 同源）
 *
 * 同意时服务端按此版本写入 `consent_log.policy_version`（ADR-012 决策 1），用于证明
 * 「用户当时同意的是哪一版文案」。P1 不实现「版本升级 → 进行中配对重新征得同意」：
 * 配对生命周期仅 30 天，创建 / 同意时锁定版本，新建配对一律取当前版本（ADR-012 决策 3）。
 */
export const INVITE_DATA_CONSENT_VERSION = '1.0';

/** 说明页标题（与 pages.json 的「双人数据处理说明」同措辞，便于用户把入口与落地页对上） */
export const INVITE_DATA_NOTICE_TITLE = '双人数据处理说明';

/**
 * R8 知情同意原句 —— **逐字保留，禁止改写**
 *
 * 服务端 `INVITE_CONSENT_TEXT` 与本常量必须逐字一致（ADR-005 已定「R8 原文，逐字一致」）。
 * 被邀请方侧由服务端通过 `InviteInviteeView.consentText` 下发同一句；
 * 本常量供**发起方在创建配对之前**事先知悉同一份可见范围约定（ADR-012 决策 2）。
 */
export const INVITE_DATA_CONSENT_TEXT =
  '你们的答案将共同生成一份关系分析；详细分析由发起人持有，你可见基础摘要。';

/**
 * 说明要点（渲染为条目列表，逐条中性陈述，不含任何关系判词 —— P7 禁词红线）
 *
 * 逐条对齐规格，顺序不可随意调换（从「数据是什么」到「你的权利」）：
 */
export const INVITE_DATA_NOTICE_POINTS: readonly string[] = [
  // ① R8 原句范围 + 三层可见性（R3）：双方所见范围不同，这是发起方也被明确告知的关键点
  '你们各自作答，答案共同用于生成一份关系分析；发起人可见完整分析，被邀请人可见基础摘要。',
  // ② 目的限制（P3 数据最小化）：只用于生成这份分析，不做二次利用
  '这次配对的数据只用于生成这份分析，不用于任何其他目的。',
  // ③ 存储地（与《隐私政策》同源，不写比政策更强的承诺）
  '数据加密存储在中国境内。',
  // ④ 配对生命周期 30 天（C4）
  '一次配对的有效期是 30 天，到期后邀请失效。',
  // ⑤ 任一方均可删除（ADR-011 决策 2）：删除的是双方共有物，不是只删自己那份
  '你或对方都可以随时删除这次配对的数据，删除后对方也无法再看到。',
  // ⑥ 删除范围（ADR-011 决策 3 的「删除」表，用用户语言表述）
  '删除会移除双方的答卷、答案快照、对比报告与纪念卡。',
  // ⑦ 保留的三类留痕及其理由（ADR-011 决策 3 的「不删除」表 + ADR-012 决策 5：必须在说明里明示）
  '删除仍会保留三类不含作答内容的记录：本次删除操作的操作留痕（用于安全审计）、数据可见性记录，以及你这次的同意记录（用于证明我们依法取得过同意）。',
  // ⑧ 单人测评卷不受影响（scene = single / p16 不在级联删除范围内）
  '你的单人测评答卷不受影响，只有这次配对产生的数据会被删除。',
];

/**
 * 同意勾选框文案
 * 默认不勾选、且**不落本地存储**：每次创建配对都要重新勾选（同意是每次都要发生的合规动作，
 * 记住「已同意过」会让第二次创建失去留证依据）。
 */
export const INVITE_DATA_CONSENT_CHECKBOX_TEXT = '我已阅读并同意《双人数据处理说明》';

/** 说明页主按钮文案（未勾选时按钮不可点，CTA 与勾选联动） */
export const INVITE_DATA_CONSENT_AGREE_TEXT = '同意并继续';

/** 说明页次级按钮文案（返回上一页，不创建配对、不产生任何留证） */
export const INVITE_DATA_CONSENT_DECLINE_TEXT = '暂不同意';

/** 版本号展示前缀（页面底部小字，页面拼成「版本 v1.0」；版本号本身只取上面的常量，不硬编码） */
export const INVITE_DATA_CONSENT_VERSION_PREFIX = '版本 v';

/**
 * 创建被服务端以 10001 拒绝时的兜底提示
 * 文案与服务端 `INVITE_DATA_CONSENT_REQUIRED_MESSAGE` 一致，指向同一份说明，避免两处措辞漂移。
 */
export const INVITE_DATA_CONSENT_REQUIRED_TIP = '请先阅读并同意《双人数据处理说明》';
