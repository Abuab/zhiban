# 品牌展示配置（唯一真源，ADR-002 决策 3）
# 本文件不含任何密钥，故允许入库；密钥/本机覆盖请写 .env.local 或 .env.<mode>.local（已被 .gitignore 忽略）
#
# 生效方式：
#   1. 构建期：scripts/inject-brand.mjs 读取以下变量写入 src/manifest.json / src/pages.json
#      （uni-app 的这两个 JSON 在编译期被写入小程序包，无法在运行时插值）
#   2. 运行时：小程序启动后由服务端 GET /api/v1/config/public 下发并覆盖展示文案
#
# 改品牌名的顺序：先在管理后台（sys_config.brand.name）改运行时文案，
# 再改此处并重新发版 —— 小程序对外名称以微信公众平台设置为准。
VITE_BRAND_NAME=知伴
VITE_BRAND_DESCRIPTION=知伴 · 双人关系测评小程序
